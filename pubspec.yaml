name: team_scheduler
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  # State Management
  flutter_bloc: ^8.1.6
  bloc: ^8.1.4
  
  # Supabase
  supabase_flutter: ^2.5.6
  
  # UI & Utilities
  image_picker: ^1.0.7
  intl: ^0.19.0
  uuid: ^4.4.0
  
  # HTTP & Storage
  http: ^1.2.1
  
  # Date & Time
  timezone: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
